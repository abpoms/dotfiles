;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/Users/abpoms/.emacs.d/plugins/emacs-for-python/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 44)
        (left . 0)
        (width . 202)
        (height . 54)))

